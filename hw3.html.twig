<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ title }}</title>
    {% block stylesheets %}
    <link rel="stylesheet" type="text/css" href="./css/hw3.css" /> 
    {% endblock %}
</head>
<body>
<div style="text-align:center">
  <h2>Tabbed Image Gallery</h2>
  <p>Click on the images below:</p>
</div>

<div style="text-align:center">
<form action="hw3_upload.php" method="post" enctype="multipart/form-data">
<p>Select image to upload:</p>
  <input type="file" name="fileToUpload" id="fileToUpload">
  <input type="submit" value="Upload Image" name="submit">
</form>
</div>

<!-- The four columns -->
<div class="row">
    {% for image in dirs %}
        {% if image != '.' and image != '..' %}
        <div class="column">
            <img src="./images/{{ image }}" alt="{{ image }}" style="width:100%" onclick="window.open(this.src)">
        </div>
        {% endif %}

    {% endfor %}   
</div>    
</body>
</html>
